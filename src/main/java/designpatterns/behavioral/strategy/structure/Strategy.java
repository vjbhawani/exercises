package designpatterns.behavioral.strategy.structure;

/**
 * behavioral pattern
 * -------------------
 * deals with distribution of algorithm and responsibilities(behavior) between object.
 * it not only describes the pattern of objects but also the pattern of communication.
 *
 * Strategy
 * --------
 * define a family of algorithms, and make them interchangeable, this pattern lets the algorithm vary based on client.
 * also known as Policy Pattern.
 */
public interface Strategy {
}
