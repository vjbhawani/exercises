package designpatterns.behavioral.state.pattern;

/**
 * note:: applicability: when an objects(context) behaviour depends upon it's state, and the behavior must change at run time depending on current AtmState
 */
public interface State {
}
