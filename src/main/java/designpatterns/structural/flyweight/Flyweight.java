package designpatterns.structural.flyweight;

/**
 * flyweight derives it's name from boxing category flyweight, which is light-weight category.
 *  note: patterns wise it means sharing light-weight/fine-grained objects, to reduce the space.
 *  fine-grained objects can be cached, instead of generating new objects
 */
public class Flyweight {
}
